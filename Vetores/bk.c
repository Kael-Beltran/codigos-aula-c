#include <stdio.h>

int main () {
    
    char lanches[4] [20] = {
        "Rodeio", "Whopper",
        "Cheddar Duplo", "Bif King"
    };

    printf("---Meus lanches favoritos---");

    for (int i=0; i < 4; i++) {
        printf("\n %s", lanches[1]);
    }


    return 0;
}