#include <stdio.h>

int main() {
    // declaração de variaveis 
    int nota1 = 8;
    int nota2 = 7;
  
    int media = (nota1 + nota2) / 2;

    printf("A media sua e %d", media);
  
    return 0;
}